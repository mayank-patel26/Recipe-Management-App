<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RecipeManagementApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorOnSecondary">@color/sandal</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/sandal</item>
        <!-- Customize your theme here. -->
        <!--Background image-->
        <item name="android:windowBackground">@drawable/blurry_background</item>

    </style>
</resources>